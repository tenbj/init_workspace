# 自查清单

生成 SQL 写回文件前，逐项核对，全部通过才能继续。

## 结构

- [ ] INSERT 目标字段数 = SELECT 表达式数（审计字段 3 列单独在末尾，不计入正文对齐校验）
- [ ] 审计字段三列齐全且顺序正确：`first_inserted_dt`、`etl_dt`、`last_updated_dt`
- [ ] 增量表（ilu）：有 DELETE 语句，且 DELETE 与 INSERT 使用同一时间过滤字段
- [ ] 全量表（flu）：用 `INSERT OVERWRITE`，且有显式目标字段列表

## 字段命名

- [ ] 无 camelCase 残留，全部为 snake_case
- [ ] 缩写规则已应用（amount→amt、count→cnt、quantity→qty、time/datetime→dt 等）
- [ ] 固定字段名未被改动（`local_created_time`、`last_updated_dt`、`first_inserted_dt`、`etl_dt` 等）
- [ ] SELECT 中的 alias 与 INSERT 目标字段列表逐行一一对应，顺序一致

## 维表

- [ ] 同目录 `.md` 固定包含「维表判断明细」模块，且记录疑似业务关联字段、命中/未命中信号、未自动 JOIN 原因
- [ ] 有 DIM JOIN 的：`shop_id` 同时出现在 INSERT 目标列表和 SELECT 中
- [ ] JOIN ON 条件字段名与 ODS 实际字段一致
- [ ] 非脚本标准命中、依据历史资料或经验规则补充的 DIM JOIN，JOIN 前有 `TODO_CONFIRM` 注释，并写入 `.md` 待处理项

## SQL 语法

- [ ] 最后一个业务字段后有逗号（审计字段块前），审计字段最后一行无逗号
- [ ] 所有括号配对
- [ ] 表名含 schema 前缀（`cbebg.` 或 `glcd.`）

## 字段覆盖

- [ ] ODS SQL 的每一个非审计字段在 DWD SQL 的 SELECT 中均有对应（名称已转换，原始字段无遗漏）

## 枚举与异常

- [ ] 顶部脚本告警注释（如 `⚠️ ENUM_FIELDS`、`⚠️  未检测到标准维表触发信号`）允许保留，但必须紧跟 `LLM处理结果`
- [ ] 字段重命名后的最终 INSERT 列表已二次扫描：具备枚举语义的 `_type`/`_status`/`_code`/`_flag`/`_level`/`_state`/`_mode`/`_kind`/`_category` 字段均已补 `_desc`
- [ ] 所有 `NULL as *_desc` 均有行内 `TODO_CONFIRM` 注释，并写入 `.md` 待处理项
- [ ] SQL 中所有 `TODO_CONFIRM` 均已写入同目录 `.md` 文件
