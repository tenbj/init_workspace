---
name: D03_OMM云端推送
description: 当用户要求 omm push、分享或上传 .omm 架构文档到 oh-my-mermaid 云端时使用。
---

# D03_OMM云端推送

## 这项技能解决什么问题

- 将 `.omm/` 架构文档推送到 oh-my-mermaid 云端
- 按顺序处理登录、项目关联和推送
- 用中文薄入口承接 `omm-push` 的原始流程

## 先读哪些本地知识

- 先确认用户明确允许进行云端登录、关联或上传
- 需要回看原始说明时，再读 `references/原始技能说明.md`
- 需要了解这次迁入时的本地化取舍时，再读 `references/迁移说明.md`
- 需要确认安装价值与限制时，再读 `references/适装评估.md`

## 固定动作

1. 确认用户要把 `.omm/` 架构文档推到云端或生成分享入口
2. 检查 `omm` CLI 是否可用；不可用时提示用户安装 `oh-my-mermaid`
3. 先运行 `omm share` 判断登录和项目关联状态
4. 未登录时引导 `omm login`；未关联项目时运行 `omm link`
5. 前置状态完成后运行 `omm push`，并报告上传结果和查看地址

## 什么时候再读本 skill 的 references

- 需要完整原始用法、原始边界或原始示例时，再读 `references/原始技能说明.md`
- 需要确认本次迁移来源、原始描述和本地化动作时，再读 `references/迁移说明.md`
- 需要判断是否适合启用该 skill 时，再读 `references/适装评估.md`

## 边界

- 不在用户未明确要求云端分享或上传时执行
- 不绕过登录、授权、付费限制或网络错误
- 不把原始长说明重新塞回 `SKILL.md`
- 不跳过当前项目已有的本地边界与治理约束
