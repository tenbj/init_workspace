# 适装评估

- 结论：条件安装
- 建议范围：项目级试装
- 来源仓库：`oh-my-mermaid/oh-my-mermaid`
- 来源路径：`skills/omm-view`

## 六项评估

- 新增知识价值：中等。补充 `.omm/` 架构文档的本地查看入口。
- 能力重叠度：低。查看器启动流程与通用代码能力重叠较少。
- 运行环境耦合度：中等偏高。依赖 `oh-my-mermaid` 的 `omm` CLI 与本地端口。
- 流程负担：低。只读查看，前置检查清楚。
- 触发范围：可控。只绑定 `omm view`、打开查看器和查看 `.omm/` 图谱。
- 上下文成本：低。入口薄化，原始长说明保留在 `references/原始技能说明.md`。

## 简短结论

适合作为 D01 的配套查看入口保留；不应在 `.omm/` 缺失时启动空查看器。
