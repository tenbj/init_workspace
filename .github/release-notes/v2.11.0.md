# init_workspace v2.11.0

## Highlights

- Added run-event logging for the initializer: every run appends an initialization or upgrade record to `.history/.system/更新日志.md`.
- Refreshed the initializer templates from the current live workspace and rebuilt `初始化工作区_v2.11.0.exe`.
- Added B09 release manifests so template sync and executable builds are traceable.

## What's Changed

- The initializer now detects whether the selected directory is a fresh initialization or an existing workspace upgrade before writing files.
- Upgrade confirmation text now explicitly describes existing workspace content.
- Bundled templates were refreshed from the current `AGENTS.md`, `CLAUDE.md`, `.agents/rules`, `.agents/skills`, and `.system/standards`.
- `SSO_SPEC_VERSION` is aligned to `workspace-spec.json` v1.14.0.

## Validation

- B09 update manifest generated.
- B09 build manifest generated.
- `python -m py_compile` passed for `init_workspace.py`.
- Isolated initialization/upgrade test passed.
- B01 framework check passed: 27 PASS, 0 FAIL, 1 WARN.

## Asset

Attach this file to the GitHub Release:

```text
output/00_系统治理_v1.21.0/03_代码程序_v2.11.0/dist/初始化工作区_v2.11.0.exe
```

SHA256:

```text
2C35139CF47F256D83A2AB4A4AE6D5B748D75109A0CC02B3881B21133E7C9E19
```
