# 系统记录 · 技能变更记录

> 按时间追加记录。

---

## 2026-05-07 08:57

**用户操作**：运行「初始化工作区」工具
**工具做了**：补建系统记录文件，骨架版本 v2.3.0

---

## 2026-05-07 09:30

**用户问**：skill 做个修正：将路径修改正确
**AI做了**：将 `01_同步代码仓库_v1.0` 的默认仓库路径从 `权威资料/data-assets` 修正为 `input/data-assets`，覆盖 `SKILL.md`、`references/同步说明.md`、`references/原始技能说明.md`、`scripts/sync_authoritative_data_assets.py` 四个文件

---

## 2026-05-08 14:35

**用户问**：output/ 下 DWD Skill 产物不合规，确认修复方案
**AI做了**：设计方案 A（全部纳入标准子项目），涉及 4 个 Skill（1对1DWD单表SQL生成、Doris建表语句查询、DWD字段信息Excel生成、ODS-DWD-一键生成）的 SKILL.md 和 2 个 Python 脚本的默认输出路径改造，方案待执行

---

## 2026-05-08 15:20

**用户问**：执行方案 A
**AI做了**：
- 备份 4 个 Skill 文件夹到 `.history/.agents/skills/`
- `generate_dwd_sql.py`：去除 `OUTPUT_DIR` 硬编码，`--output-dir` 改为 `required=True`
- `get_create_table.py`：去除 `DEFAULT_OUT` 硬编码，`--output-dir` 改为 `required=True`
- 4 个 SKILL.md 全部更新：新增"定位 DWD 开发产物子项目"前置步骤，输出路径引用改为子项目内路径

---

## 2026-05-09 15:39

**用户问**：目录.md 文件列加跳转链接，并将规范写入标准和框架组件；将项目所有规则内化成 AI 操作习惯
**AI做了**：修改 `子项目管理/SKILL.md`、`课题研究/SKILL.md`、`项目规范化/references/标准自查清单.md`，将目录.md 文件列链接格式改为引用 `工作区骨架规格.md`（SSOT 收敛）；新建 `CLAUDE.md` 将项目所有规则内化为 AI 跨会话操作习惯
**产出文件**：`CLAUDE.md`

---

## 2026-05-09 16:00

**用户问**：斜杠命令无法调用 Skill
**AI做了**：在 `.claude/commands/` 下为全部 15 个 Skill 各建一个命令文件，内容为引用对应 SKILL.md 的一句话 prompt
**产出文件**：`.claude/commands/*.md`（15 个）
**教训**：本次未在修改前执行版本备份，补做无意义。下次必须先备份再动文件。

---

## 2026-05-09 16:11

**用户问**：按照 Skills 调整清单执行迁移。
**AI做了**：将 15 个 Skill 文件夹、`SKILL.md` 的 YAML `name`、`agents/openai.yaml` 展示名统一迁移为 `{域编号}_{技能名}_v1.0.0`；补齐 `B02_版本控制备份_v1.0.0`、`B03_记忆管理_v1.0.0`、`B04_子项目管理_v1.0.0` 的 `agents/openai.yaml`；同步 `.claude/commands` 文件名为带版本号命令。
**产出文件**：`.agents/skills/*_v1.0.0/`、`.claude/commands/*_v1.0.0.md`

---

## 2026-05-10 13:56

**用户问**：将 Claude 部分纳入当前骨架
**AI做了**：扩展 B01 框架体检和 B06 项目规范化能力，使其识别 `requiredLayer.files`、检查 Claude 命令与注册 Skill 的一一对应关系，并在规范化 `--Fix` 时可补齐/重写命令文件。
**产出文件**：`.agents/skills/B01_框架体检_v1.0.0/scripts/framework-check.ps1`、`.agents/skills/B06_项目规范化_v1.0.0/scripts/normalize.ps1`

---

## 2026-05-10 14:53

**用户问**：希望 C01 同步 skill 在 `input` 为空时也能拉下 `data-assets`，已有仓库时继续更新。
**AI做了**：增强 `C01_同步代码仓库_v1.0.0`，使 `sync_authoritative_data_assets.py` 在目标仓库缺失或目标目录为空时可从配置的远端仓库克隆，已有仓库时沿用强制同步流程；同步更新 `SKILL.md`、`references/同步说明.md` 和 `agents/openai.yaml`。
**产出文件**：`.agents/skills/C01_同步代码仓库_v1.0.0/`

---

## 2026-05-10 15:51

**用户问**：入口文件备份需要分别进入 `.history` 下的专门文件夹。
**AI做了**：更新 `B02_版本控制备份_v1.0.0` 的 CONFIG 模式说明和脚本行为，使 `AGENTS.md` 备份进入 `.history/AGENTS/`，`CLAUDE.md` 备份进入 `.history/CLAUDE/`，其他 CONFIG 文件继续按镜像路径备份。
**产出文件**：`.agents/skills/B02_版本控制备份_v1.0.0/`

---

## 2026-05-10 16:13

**用户问**：`Skill 编号分流` 的指引在各个 skills 中还没修复，需要适配整个系统。
**AI做了**：将 `.agents/rules/` 与相关 Skill 中的跨 Skill 意图引用统一改为稳定编号（如 `B04`、`C03`、`C05`、`C06`），实际文件路径、脚本路径、产物目录和注册表仍保留完整 Skill 名称；同步修正 `C01` 的 OpenAI 默认调用提示为 `/C01`。
**产出文件**：`.agents/rules/*.md`、`.agents/skills/B03_记忆管理_v1.0.0/`、`.agents/skills/B04_子项目管理_v1.0.0/`、`.agents/skills/B05_课题研究_v1.0.0/`、`.agents/skills/B06_项目规范化_v1.0.0/`、`.agents/skills/C01_同步代码仓库_v1.0.0/`、`.agents/skills/C05_1对1DWD单表SQL生成_v1.0.0/`、`.agents/skills/C07_ODS-DWD-一键生成_v1.0.0/`

---

## 2026-05-10 16:43

**用户问**：准备发布 GitHub，其他缺失内容尽量补齐。
**AI做了**：发布前安全治理 C01/C02/C04：C01 clone 地址改为 `DATA_ASSETS_REPO_URL` 或 `--clone-url` 注入；C02 查询连接信息改为 `DORIS_QUERY_*` 环境变量；C04 写入连接信息改为 `DORIS_STORAGE_*` 环境变量；同步更新 live Skill 与初始化工具模板。
**产出文件**：`.agents/skills/C01_同步代码仓库_v1.0.0/`、`.agents/skills/C02_数仓生产库查询_v1.0.0/`、`.agents/skills/C04_临时数据存储_v1.0.0/`、`output/00_系统治理_v1.6.1/03_代码程序_v2.6.1/src/templates/skills/`

---

