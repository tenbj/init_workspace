# 系统记录 · 脚本治理记录

> 按时间追加记录。

---

## 2026-05-14 15:05

**用户问**：A03 中未运行的 `detect_core_skeleton_changes.py` 脚本是什么作用。
**AI做了**：读取脚本和 A03 核心骨架识别规则，确认该脚本是只读差异检测器，用于输出 Skill 注册、Claude 命令、Rule、标准文件、BOM 脚本清单和核心路径 Git 状态的对照报告，不直接修改文件。

---

## 2026-05-14 14:55

**用户问**：同步 E01 增强版并更新版本。
**AI做了**：迁入对照项目 E01 的 `template_writeback` 脚本能力，更新内部 Skill 名称到 v1.0.2，运行 `python -m py_compile` 校验 E01 Python 脚本，并清理验证产生的 `__pycache__`。
**产出文件**：`.agents/skills/E01_模型设计标准化_v1.0.2/scripts/`

---

## 2026-05-07 08:57

**用户操作**：运行「初始化工作区」工具
**工具做了**：补建系统记录文件，骨架版本 v2.3.0

---

## 2026-05-08 13:43

**用户问**：output/ 下产生了不符合规范的子项目文件夹
**AI做了**：排查定位到 `generate_dwd_sql.py` 第 30 行硬编码输出路径 `output/1对1DWD单表SQL生成/`，单独运行时绕过子项目管理创建了不规范文件夹。需修改脚本默认路径或要求 AI 在调用前先建标准子项目文件夹

---

## 2026-05-07 09:05

**用户问**：同步代码仓库
**AI做了**：运行 `sync_authoritative_data_assets.py --repo-relative input/data-assets`，强制对齐远端 master（ad6da564→507d4d48），push 已设为 DISABLED
**备注**：实际仓库路径为 `input/data-assets` 而非 skill 默认的 `权威资料/data-assets`，需用 `--repo-relative` 参数指定；脚本运行时线程有 UnicodeDecodeError（GBK 编码），不影响 git 操作结果

---

## 2026-05-11 21:56

**用户问**：说明“模糊问题原子化识别过程”时触发系统治理子项目备份。
**AI做了**：执行 B02 PROJECT 备份时，live 子项目已成功升级到 `00_系统治理_v1.9.0`，但历史区快照末尾重命名出现 `Access denied`，留下 `.history/output/00_系统治理_v1.8.0` 未加时间戳目录；本轮未手工移动 `.history`，保留为后续脚本治理排查项。

---

## 2026-05-09 16:11

**用户问**：按照 Skills 调整清单执行迁移。
**AI做了**：同步更新 `framework-check.ps1` 与 `normalize.ps1` 的 fallback Skill 名和 BOM 脚本路径；修复机械替换误把子项目分类 `课题研究` 改成 Skill 名的问题，并保持 `.ps1` 为 UTF-8 with BOM。
**产出文件**：`B01_框架体检_v1.0.0/scripts/framework-check.ps1`、`B06_项目规范化_v1.0.0/scripts/normalize.ps1`

---

## 2026-05-09 16:50

**用户问**：标准内文件格式不一致，怎么规范
**AI做了**：执行 `normalize.ps1 --Check`，确认 `.system/standards/03_Skills管理标准_v1.0.0.md` 为未定稿草案且未列入 `workspace-spec.json` 标准清单；按规范备份后将其迁入 `output/00_系统治理_v1.2.0/02_课题研究_v1.1.0/02_Skills管理标准草案_v1.0.0.md`，并复跑检查无 FAIL。
**剩余待治理项**：C01-C07 额外 Skill 是否纳入核心覆盖层清单；`.history/output/1对1DWD单表SQL生成` 旧快照缺时间戳。

---

## 2026-05-09 17:45

**用户问**：Skills 管理标准应作为正式 SSOT，检查当前 Skill 指向并系统性适配
**AI做了**：修正 `normalize.ps1` 的 Skill 合法性判断：核心覆盖 Skill 继续看 `overwriteLayer.skills`，全部合法 Skill 改看 `skillsManagement.registeredSkills`；同步 `framework-check.ps1` fallback 标准文件清单，补入 `Skills管理标准.md`，并保持两个 `.ps1` 为 UTF-8 with BOM。
**验证结果**：15 个注册 Skill 与实际目录一致，所有 `SKILL.md` YAML `name` 与文件夹名一致；框架体检 0 FAIL，规范化检查 0 FAIL。

---

## 2026-05-10 11:44

**用户问**：将 `output/05_工作区初始化工具_v1.17.0` 按规则合并进 00 子项目
**AI做了**：把初始化工具当前态归入 `00_系统治理_v1.4.0`：问答文档进入 `01_问题答疑_v1.0.0`，源码、构建脚本、模板和 EXE 进入 `03_代码程序_v2.4.0`，迁移前版本记录归档到 `02_课题研究_v1.3.0`；移除 05 独立当前态并复跑 `normalize.ps1 --Check` 为 0 FAIL。
**产出文件**：`output/00_系统治理_v1.4.0/`、`.memory/知识提炼/系统治理.md`

---

## 2026-05-10 13:56

**用户问**：重新识别骨架并评估 Claude 部分是否纳入标准
**AI做了**：将初始化工具升级到 v2.5.0，新增 Claude 接入层初始化逻辑，模板同步 15 个版本化 Skill、4 个标准文件和 `CLAUDE.md`，并用 PyInstaller 构建 `初始化工作区_v2.5.0.exe`；验证临时初始化可生成 15 个 Claude 命令。
**产出文件**：`output/00_系统治理_v1.5.0/03_代码程序_v2.5.0/src/init_workspace.py`、`output/00_系统治理_v1.5.0/03_代码程序_v2.5.0/dist/初始化工作区_v2.5.0.exe`

---

## 2026-05-10 14:12

**用户问**：`.Claude.json` 需要在 EXE 中，但要去掉 URL 和 token
**AI做了**：将初始化工具升级到 v2.5.1，新增 `src/templates/.Claude.json` 脱敏模板和 `src/templates/.claude/settings.local.json` 空权限模板；`init_workspace.py` 优先使用 EXE 内置模板补缺，仍不覆盖已有真实本机配置；重新构建 EXE 并验证脱敏结果。
**产出文件**：`output/00_系统治理_v1.5.1/03_代码程序_v2.5.1/src/templates/.Claude.json`、`output/00_系统治理_v1.5.1/03_代码程序_v2.5.1/dist/初始化工作区_v2.5.1.exe`

---

## 2026-05-10 14:53

**用户问**：C01 同步脚本需要支持 `input/data-assets` 缺失时自动拉取仓库。
**AI做了**：为 `sync_authoritative_data_assets.py` 新增默认 clone URL、`--clone-url` 参数、项目根目录边界校验和缺失/空目录自动 clone 分支；非空非 Git 目录会停止报错以避免误删未知内容。已通过 `py_compile`、`--help`、现有仓库 `--status-only` 和临时本地 Git 远端 clone 烟测。
**产出文件**：`.agents/skills/C01_同步代码仓库_v1.0.0/scripts/sync_authoritative_data_assets.py`

---

## 2026-05-10 15:51

**用户问**：入口文件历史备份不要平铺在 `.history` 根目录，`AGENTS.md` 与 `CLAUDE.md` 都要有专门目录。
**AI做了**：调整 `backup.ps1` 的 CONFIG 模式，根入口 `AGENTS.md` 特殊备份到 `.history/AGENTS/`，`CLAUDE.md` 特殊备份到 `.history/CLAUDE/`；用 UTF-8 with BOM 写回脚本并验证 BOM 未丢失。
**产出文件**：`.agents/skills/B02_版本控制备份_v1.0.0/scripts/backup.ps1`

---

## 2026-05-10 16:13

**用户问**：确认修正完成后更新骨架文件，重新 00 里的程序，并更新 exe 文件。
**AI做了**：将 `00_系统治理` 升至 v1.6.0，初始化工具升至 v2.6.0；`init_workspace.py` 新增 `AGENTS.md` 初始化/覆盖、`.history/AGENTS` 与 `.history/CLAUDE` 专属备份目录，模板同步当前入口、规则、Skill 和标准；用 PyInstaller 构建 `初始化工作区_v2.6.0.exe` 并用临时工作区验证模板初始化。
**产出文件**：`output/00_系统治理_v1.6.0/03_代码程序_v2.6.0/src/init_workspace.py`、`output/00_系统治理_v1.6.0/03_代码程序_v2.6.0/src/templates/`、`output/00_系统治理_v1.6.0/03_代码程序_v2.6.0/dist/初始化工作区_v2.6.0.exe`

---

## 2026-05-10 16:43

**用户问**：如果可以，需要将 exe 文件发布至 Release。
**AI做了**：将 `00_系统治理` 升至 v1.6.1，初始化工具升至 v2.6.1；同步脱敏后的 C01/C02/C04 模板并重新构建 `初始化工作区_v2.6.1.exe`，作为待上传 GitHub Release 的资产。
**产出文件**：`output/00_系统治理_v1.6.1/03_代码程序_v2.6.1/src/init_workspace.py`、`output/00_系统治理_v1.6.1/03_代码程序_v2.6.1/src/templates/`、`output/00_系统治理_v1.6.1/03_代码程序_v2.6.1/dist/初始化工作区_v2.6.1.exe`

---

## 2026-05-11 03:33

**用户问**：更新骨架、更新 00 程序和 exe，并发布 GitHub Release。
**AI做了**：将初始化工具升至 v2.7.0，模板同步当前 18 个 Skill（含 D01/D02/D03 OMM 链路）、4 个 Rule、4 个标准文件和最新入口；`.Claude.json` 模板继续保持空 env。重建 `初始化工作区_v2.7.0.exe`，提交并推送 `b794b61`，创建 `v2.7.0` release，上传 `init_workspace_v2.7.0.exe`。
**验证结果**：源码初始化隔离测试通过；框架体检 20 PASS、0 FAIL；`omm validate` 通过；Release 附件 SHA256 为 `502FBB3C992C8EE3423855AB9E73AEAB5BFFFCFB628BD15C2609A684954F1E25`。
**产出文件**：`output/00_系统治理_v1.8.0/03_代码程序_v2.7.0/src/init_workspace.py`、`output/00_系统治理_v1.8.0/03_代码程序_v2.7.0/src/templates/`、`output/00_系统治理_v1.8.0/03_代码程序_v2.7.0/dist/初始化工作区_v2.7.0.exe`

---

## 2026-05-11 21:52

**用户问**：执行目录链接规范最小整改方案。
**AI做了**：修改 `framework-check.ps1`，新增 `Catalog Link Format` 检查：遍历 `output/*/目录.md` 的 `01/02` 分节，报告非 Markdown 链接文件列和不存在的链接目标；写回时保持 UTF-8 with BOM。
**产出文件**：`.agents/skills/B01_框架体检_v1.0.0/scripts/framework-check.ps1`

---

## 2026-05-11 22:02

**用户问**：执行任务进度管理体系设计方案。
**AI做了**：新增 `task_progress.ps1` 的 Start/Checkpoint/UpdateNext/Pause/Close/Resume 动作，并扩展 `framework-check.ps1` 检查 `.memory/任务进度` 结构、索引链接和未索引 active 任务；两个脚本均保持 UTF-8 with BOM。
**验证结果**：B08 `Resume/Start/Checkpoint` 可运行；B01 体检 23 PASS、0 FAIL、1 WARN。
**产出文件**：`.agents/skills/B08_任务进度管理_v1.0.0/scripts/task_progress.ps1`、`.agents/skills/B01_框架体检_v1.0.0/scripts/framework-check.ps1`

---

## 2026-05-11 22:58

**用户问**：给 B05 补充根因能力后同步版本引用。
**AI做了**：将 `framework-check.ps1` 与 `normalize.ps1` 中覆盖层 Skill fallback 列表的 `B05_课题研究_v1.0.0` 替换为 `B05_课题研究_v1.1.0`，并用 UTF-8 with BOM 写回；B01 验证 BOM 与框架体检均通过。
**产出文件**：`.agents/skills/B01_框架体检_v1.0.0/scripts/framework-check.ps1`、`.agents/skills/B06_项目规范化_v1.0.0/scripts/normalize.ps1`

---

## 2026-05-11 23:02

**用户问**：AI 每次运行读取中文文件有乱码，是否可以把默认命令改成 `Get-Content -Encoding UTF8 -Raw`。
**AI做了**：验证 Windows PowerShell 5.1 下无 BOM UTF-8 文件使用默认 `Get-Content -Raw` 会乱码，显式 `-Encoding UTF8 -Raw` 正常；产出默认读取编码治理方案，建议入口约束、执行层示例收敛和 B01 检查联动。
**产出文件**：`output/00_系统治理_v1.13.0/02_课题研究_v1.8.0/09_默认读取编码治理方案_v1.0.0.md`

---

## 2026-05-11 23:27

**用户问**：修复 E01 调用时不符合 SSOT 的输出子项目定位问题。
**AI做了**：修改 E01 的 `e01_common.py`，将默认输出子项目发现从硬编码 `output/01_模型设计标准化_v*` 改为动态匹配 `output/*_模型设计标准化_v*` 并按版本选择当前 live 子项目；验证结果能解析到 `output/03_模型设计标准化_v1.9.0`。
**产出文件**：`.agents/skills/E01_模型设计标准化_v1.0.1/scripts/e01_common.py`

---

## 2026-05-12 00:39

**用户问**：为 `.history/.system` 增加更新日志，并让 B02/B01 配套调整。
**AI做了**：修改 `backup.ps1`，新增 `.system` 快照日志追加函数、快照内文件版本识别和根目录独立文件历史目录路由；修改 `framework-check.ps1`，新增 `.system` 更新日志覆盖检查、README 历史目录检查，并收紧 Get-Content 编码检查的假阳性匹配。
**验证结果**：两个 `.ps1` 均保持 UTF-8 with BOM，Parser 0 error；B01 体检 26 PASS、0 FAIL、1 WARN。
**产出文件**：`.agents/skills/B02_版本控制备份_v1.0.0/scripts/backup.ps1`、`.agents/skills/B01_框架体检_v1.0.0/scripts/framework-check.ps1`

---

## 2026-05-12 00:59

**用户问**：执行 Skill YAML `name` 命名硬门整改。
**AI做了**：为 A01 `init_skill.py`/`quick_validate.py`、A02 `install-skill-from-github.py`、B01 `framework-check.ps1`、B06 `normalize.ps1` 增加标准 Skill 名、YAML `name` 等于文件夹名、description 不超过 100 字与注册表一致性校验，并重建初始化工具 exe v2.9.0。
**验证结果**：Python 编译通过；`.ps1` Parser 0 error 且 BOM 保持；B01 27 PASS、0 FAIL、1 WARN；B06 50 PASS、0 FAIL、6 WARN；`初始化工作区_v2.9.0.exe` 构建成功。
**产出文件**：`.agents/skills/A01_创建技能_v1.0.0/scripts/init_skill.py`、`.agents/skills/A01_创建技能_v1.0.0/scripts/quick_validate.py`、`.agents/skills/A02_安装技能_v1.0.0/scripts/install-skill-from-github.py`、`.agents/skills/B01_框架体检_v1.0.0/scripts/framework-check.ps1`、`.agents/skills/B06_项目规范化_v1.0.0/scripts/normalize.ps1`

---

## 2026-05-12 01:29

**用户问**：重新检查 A01/A02 中 `display_name` 没按完整标准名生成的问题。
**AI做了**：A01 禁止通过 `--interface display_name=...` 覆盖展示名，A02 迁入时不再从标准名剥离中文短名；B01/B06 的 Skill Naming Contract 新增 `agents/openai.yaml`、`display_name`、`short_description` 校验，并将模板层同步到初始化工具 v2.10.0。
**验证结果**：短 `display_name` 覆盖会被 A01 拒绝；合法新建 Skill 的 quick_validate 通过；模板 24 个 Skill 的 `display_name` 全部等于文件夹名；B01 27 PASS、0 FAIL、1 WARN；B06 50 PASS、0 FAIL、6 WARN。
**产出文件**：`.agents/skills/A01_创建技能_v1.0.0/scripts/generate_openai_yaml.py`、`.agents/skills/A01_创建技能_v1.0.0/scripts/quick_validate.py`、`.agents/skills/A02_安装技能_v1.0.0/scripts/install-skill-from-github.py`、`.agents/skills/B01_框架体检_v1.0.0/scripts/framework-check.ps1`、`.agents/skills/B06_项目规范化_v1.0.0/scripts/normalize.ps1`、`output/00_系统治理_v1.17.0/03_代码程序_v2.10.0/dist/初始化工作区_v2.10.0.exe`

---

## 2026-05-12 02:20

**用户问**：当前 exe 会覆盖 `.memory` 和 `.history` 吗？
**AI做了**：核验初始化工具 v2.10.0 的 `init_workspace.py` 与 `build.ps1`，确认 `.memory` 采用 `write_if_missing()` 补缺，`.history` 采用目录补建与追加备份，不执行整体覆盖或清空。
**验证结果**：B01 体检 27 PASS、0 FAIL、1 WARN；源码显示真正覆盖范围为受管 Rule、Skill、标准、入口文件和 Claude 命令。
**产出文件**：`output/00_系统治理_v1.18.0/01_问题答疑_v1.3.0/06_当前exe是否覆盖memory和history_v1.0.0.md`

---

## 2026-05-12 02:29

**用户问**：每次运行 exe 应在 `.system` 更新日志中体现初始化或升级，询问实现方式。
**AI做了**：提出初始化工具运行事件日志方案：新增操作类型探测函数，扩展 `initialize_workspace()` 传递运行类型，在流程末尾向 `.history/.system/更新日志.md` 追加 `init_workspace_run_{timestamp}` 条目，并保持 B02 快照日志与 EXE 运行日志分型。
**验证结果**：方案要求空目录运行显示“初始化”，已有工作区运行显示“升级”，B01 不应将 `init_workspace_run_*` 误判为 `.system` 快照目录。
**产出文件**：`output/00_系统治理_v1.19.0/02_课题研究_v1.10.0/11_EXE运行写入system更新日志方案_v1.0.0.md`

---

## 2026-05-12 02:39

**用户问**：执行 EXE 运行写入 `.system` 更新日志方案。
**AI做了**：修改 `init_workspace.py`，新增 `detect_operation_type()` 与 `append_system_run_log()`，在 `initialize_workspace()` 末尾追加运行事件日志；同步 `build.ps1` 到 v2.11.0 并重建 EXE。
**验证结果**：`init_workspace.py` 通过 `py_compile`；`build.ps1` Parser 0 error 且保持 UTF-8 with BOM；隔离测试确认首次运行写“初始化”、二次运行写“升级”；`初始化工作区_v2.11.0.exe` 构建成功，SHA256 `09C3A725DF02F7C5BA2CB8A18A0E8CB68164EA7D5F0A6643A5EABF6C1806BF2D`；B01 体检 27 PASS、0 FAIL、1 WARN。
**产出文件**：`output/00_系统治理_v1.20.0/03_代码程序_v2.11.0/src/init_workspace.py`、`output/00_系统治理_v1.20.0/03_代码程序_v2.11.0/src/build.ps1`、`output/00_系统治理_v1.20.0/03_代码程序_v2.11.0/dist/初始化工作区_v2.11.0.exe`

---
## 2026-05-12 03:03

**用户问**：解除占用并继续执行 B09 GitHub 发布准备。
**AI做了**：运行 B09 的 `update_init_program.ps1` 与 `build_init_exe.ps1`，刷新初始化程序模板、生成 `.b09_update_manifest.json` / `.b09_build_manifest.json`，并通过 py_compile 与隔离初始化/升级日志测试。
**产出文件**：`output/00_系统治理_v1.21.0/03_代码程序_v2.11.0/src/.b09_update_manifest.json`；`output/00_系统治理_v1.21.0/03_代码程序_v2.11.0/dist/.b09_build_manifest.json`

---

## 2026-05-12 03:19

**用户问**：允许提交、打 tag、推送并发布 GitHub Release。
**AI做了**：完成 B09 发布：提交 `b61cfc9`，推送 `main` 与 `v2.11.0`，通过 GitHub API 创建 Release 并上传 EXE 附件；GitHub 将非 ASCII 附件文件名规整为 `_v2.11.0.exe`，已给附件补中文 label。
**产出文件**：`https://github.com/tenbj/init_workspace/releases/tag/v2.11.0`

---
