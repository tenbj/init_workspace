# 适装评估

- 结论：条件安装
- 建议范围：项目级试装
- 来源仓库：`oh-my-mermaid/oh-my-mermaid`
- 来源路径：`skills/omm-push`

## 六项评估

- 新增知识价值：中等。补充 `.omm/` 架构文档云端发布、分享和错误处理流程。
- 能力重叠度：低。云端推送、登录和项目关联属于专用工作流。
- 运行环境耦合度：高。依赖 `omm` CLI、GitHub OAuth、网络状态和 oh-my-mermaid 云端服务。
- 流程负担：中等。涉及授权和外部服务，必须由用户明确触发。
- 触发范围：严格收窄。只绑定 `omm push`、云端上传、分享架构文档。
- 上下文成本：可控。入口薄化，原始长说明保留在 `references/原始技能说明.md`。

## 简短结论

可作为可选云端发布能力保留，但默认不能主动触发上传、登录或分享流程。
