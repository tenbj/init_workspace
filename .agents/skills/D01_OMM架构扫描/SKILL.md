---
name: D01_OMM架构扫描
description: 当用户要求 omm scan、扫描或刷新架构图、生成 .omm 架构文档时使用；按需展开原始说明。
---

# D01_OMM架构扫描

## 这项技能解决什么问题

- 扫描代码结构，生成或更新 `.omm/` 架构文档
- 按视角拆分 Mermaid 图，并递归补充节点说明
- 用中文薄入口承接 `omm-scan` 的原始流程

## 先读哪些本地知识

- 先确认当前仓库是否适合生成 `.omm/` 架构文档
- 需要回看原始说明时，再读 `references/原始技能说明.md`
- 需要了解这次迁入时的本地化取舍时，再读 `references/迁移说明.md`
- 需要确认安装价值与限制时，再读 `references/适装评估.md`

## 固定动作

1. 确认用户明确要求扫描、生成或刷新架构文档
2. 检查 `omm` CLI 是否可用；不可用时提示用户安装 `oh-my-mermaid`
3. 阅读项目入口、依赖清单和关键目录，选择必要视角
4. 按原始说明用 `omm write` 生成各视角字段与递归节点
5. 汇总本次创建或更新的 `.omm/` 内容，并建议用 D02 查看

## 什么时候再读本 skill 的 references

- 需要完整原始用法、原始边界或原始示例时，再读 `references/原始技能说明.md`
- 需要确认本次迁移来源、原始描述和本地化动作时，再读 `references/迁移说明.md`
- 需要判断是否适合启用该 skill 时，再读 `references/适装评估.md`

## 边界

- 不在用户未要求架构扫描时主动生成 `.omm/`
- 不强制安装全局 npm 包；安装失败时让用户在终端处理
- 不把原始长说明重新塞回 `SKILL.md`
- 不跳过当前项目已有的本地边界与治理约束
